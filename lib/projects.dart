import 'package:flutter/material.dart';

class Projects extends StatelessWidget {
  final String title;

  const Projects({super.key, required this.title});

  @override
  Widget build(BuildContext context) {
    return ListView(
      children: const [
        Text("tom"),
        Text("tom"),
        Text("tom"),
        Text("tom"),
        Text("tom"),
        Text("tom"),
      ],
    );
  }
}
